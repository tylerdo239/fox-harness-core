{
  "name": "@fox-harness/profile-template",
  "private": true,
  "version": "0.0.1",
  "description": "Template files copied into $DSH_HOME/profiles/<name>/ by the orchestrator's init step. Not a dsh bundle, not auto-discovered — see README.md."
}
